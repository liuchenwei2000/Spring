
Spring可以管理singleton作用域Bean的生命周期，可以精确的知道该Bean何时被创建、何时被初始化完成、何时准备被容器销毁。
对于prototype作用域的Bean，Spring容器仅仅负责创建实例，当容器创建了Bean实例之后，Bean实例完全交给客户端代码管理，容器不再跟踪其生命周期。

Spring可以管理实例化结束之后和销毁实例之前的行为，主要有如下两个时机：
1，注入依赖关系之后。
2，即将销毁Bean之前。
