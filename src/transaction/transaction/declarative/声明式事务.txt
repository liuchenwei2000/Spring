
Spring提供了对POJO的声明式事务的支持，这是Spring的一个重大特性，因为现在声明原子操作，有了EJB容器之外的选择。

Spring对声明式事务管理的支持是通过它的AOP框架来实现的，因为事务是系统级的服务，凌驾于应用程序的主要功能之上。
可以把Spring的事务想象成是一个切面，用事务性边界包裹着一个方法。

Spring提供了三种配置声明式事务的方式：
1，使用Spring的 TransactionProxyFactoryBean。
2，使用简单的XML声明（XML-declared）。
3，使用注释驱动（annotation-driven）事务。