
对于大多数情况，可以将整个应用程序配置在一个 Bean 装配文件中。但有时候，将一部分配置抽取到单独的属性文件中会更好。例如，对于很多应用程序来说都会配置的数据源。
Spring 自带了几个选项，可以借助它们将配置细节信息外部化到属性文件中，这样就能在部署的应用之外尽心管理：

属性占位符配置（Property placeholder configurer）会将占位符内的变量替换为外部属性文件的值。
属性重写（Property overrider）会将 Bean 属性的值用外部属性文件的值进行重写。
